# Django
SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (optional; default is SQLite)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=nura_stays
# DATABASE_USER=
# DATABASE_PASSWORD=
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Contact form: email address to receive submissions
CONTACT_EMAIL_TO=syedbilaln786@gmail.com

# Gmail SMTP (for sending contact form emails)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=syedbilaln786@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=syedbilaln786@gmail.com

# Optional: Media root for Cloud Run
# MEDIA_ROOT=/tmp/media
